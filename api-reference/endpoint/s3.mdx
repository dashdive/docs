---
title: 'S3'
openapi: 'POST /s3'
---

**S3 Event Notification**: This endpoint receives notifications for events happening in an S3 bucket, like file uploads.


# Body Options

<ResponseField name="ListBuckets" type="1">
  <Expandable title="Properties">
    <ResponseField name="action" type="'ListBuckets'">
      The type of action, which is 'ListBuckets'.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="DeleteBucket" type="2">
  <Expandable title="Properties">
    <ResponseField name="action" type="'DeleteBucket'">
      The type of action, which is 'DeleteBucket'.
    </ResponseField>
    <ResponseField name="force" type="boolean">
      A boolean indicating whether the deletion should be forced.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket to be deleted.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="HeadBucket" type="3">
  <Expandable title="Properties">
    <ResponseField name="action" type="'HeadBucket'">
      The type of action, which is 'HeadBucket'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket to be accessed.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="CreateBucket" type="4">
  <Expandable title="Properties">
    <ResponseField name="action" type="'CreateBucket'">
      The type of action, which is 'CreateBucket'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket to be created.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="GetBucketAcl" type="5">
  <Expandable title="Properties">
    <ResponseField name="action" type="'GetBucketAcl'">
      The type of action, which is 'GetBucketAcl'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the ACL (Access Control List) is being retrieved.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="GetBucketCors" type="6">
  <Expandable title="Properties">
    <ResponseField name="action" type="'GetBucketCors'">
      The type of action, which is 'GetBucketCors'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the CORS (Cross-Origin Resource Sharing) configuration is being retrieved.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="GetBucketEncryption" type="7">
  <Expandable title="Properties">
    <ResponseField name="action" type="'GetBucketEncryption'">
      The type of action, which is 'GetBucketEncryption'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the encryption settings are being retrieved.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="GetBucketLocation" type="8">
  <Expandable title="Properties">
    <ResponseField name="action" type="'GetBucketLocation'">
      The type of action, which is 'GetBucketLocation'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the location information is being retrieved.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="GetBucketVersioning" type="9">
  <Expandable title="Properties">
    <ResponseField name="action" type="'GetBucketVersioning'">
      The type of action, which is 'GetBucketVersioning'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the versioning information is being retrieved.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="PutBucketAcl" type="10">
  <Expandable title="Properties">
    <ResponseField name="action" type="'PutBucketAcl'">
      The type of action, which is 'PutBucketAcl'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the ACL (Access Control List) is being set.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="PutBucketCors" type="11">
  <Expandable title="Properties">
    <ResponseField name="action" type="'PutBucketCors'">
      The type of action, which is 'PutBucketCors'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the CORS (Cross-Origin Resource Sharing) settings are being set.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="PutBucketEncryption" type="12">
  <Expandable title="Properties">
    <ResponseField name="action" type="'PutBucketEncryption'">
      The type of action, which is 'PutBucketEncryption'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the encryption settings are being set.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="DeleteBucketCors" type="13">
  <Expandable title="Properties">
    <ResponseField name="action" type="'DeleteBucketCors'">
      The type of action, which is 'DeleteBucketCors'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the CORS (Cross-Origin Resource Sharing) settings are being deleted.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="DeleteBucketEncryption" type="14">
  <Expandable title="Properties">
    <ResponseField name="action" type="'DeleteBucketEncryption'">
      The type of action, which is 'DeleteBucketEncryption'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the encryption settings are being deleted.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="GetObjectLockConfiguration" type="15">
  <Expandable title="Properties">
    <ResponseField name="action" type="'GetObjectLockConfiguration'">
      The type of action, which is 'GetObjectLockConfiguration'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the object lock configuration is being retrieved.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="PutObjectLockConfiguration" type="16">
  <Expandable title="Properties">
    <ResponseField name="action" type="'PutObjectLockConfiguration'">
      The type of action, which is 'PutObjectLockConfiguration'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which the object lock configuration is being set.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ListObjectsV2" type="17">
  <Expandable title="Properties">
    <ResponseField name="action" type="'ListObjectsV2'">
      The type of action, which is 'ListObjectsV2'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket for which objects are being listed.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="HeadObject" type="18">
  <Expandable title="Properties">
    <ResponseField name="action" type="'HeadObject'">
      The type of action, which is 'HeadObject'.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object being accessed.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket in which the object is stored.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ListObjectVersions" type="19">
  <Expandable title="Properties">
    <ResponseField name="action" type="'ListObjectVersions'">
      The type of action, which is 'ListObjectVersions'.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object for which versions are being listed.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket containing the object.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="GetObjectAcl" type="20">
  <Expandable title="Properties">
    <ResponseField name="action" type="'GetObjectAcl'">
      The type of action, which is 'GetObjectAcl'.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object for which the Access Control List (ACL) is being retrieved.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket containing the object.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="PutObjectAcl" type="21">
  <Expandable title="Properties">
    <ResponseField name="action" type="'PutObjectAcl'">
      The type of action, which is 'PutObjectAcl'.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object for which the Access Control List (ACL) is being set.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket containing the object.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="GetObjectRetention" type="22">
  <Expandable title="Properties">
    <ResponseField name="action" type="'GetObjectRetention'">
      The type of action, which is 'GetObjectRetention'.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object for which the retention information is being retrieved.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket containing the object.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="PutObjectRetention" type="23">
  <Expandable title="Properties">
    <ResponseField name="action" type="'PutObjectRetention'">
      The type of action, which is 'PutObjectRetention'.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object for which the retention policy is being set.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket containing the object.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="GetObjectLegalHold" type="24">
  <Expandable title="Properties">
    <ResponseField name="action" type="'GetObjectLegalHold'">
      The type of action, which is 'GetObjectLegalHold'.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object for which the legal hold status is being retrieved.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket containing the object.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="PutObjectLegalHold" type="25">
  <Expandable title="Properties">
    <ResponseField name="action" type="'PutObjectLegalHold'">
      The type of action, which is 'PutObjectLegalHold'.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object for which the legal hold is being set.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket containing the object.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="GetObject" type="26">
  <Expandable title="Properties">
    <ResponseField name="action" type="'GetObject'">
      The type of action, which is 'GetObject'.
    </ResponseField>
    <ResponseField name="bytes" type="integer">
      The size of the object in bytes.
    </ResponseField>
    <ResponseField name="versionId" type="string">
      The version ID of the object being retrieved.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object being retrieved.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket containing the object.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="PutObject" type="27">
  <Expandable title="Properties">
    <ResponseField name="action" type="'PutObject'">
      The type of action, which is 'PutObject'.
    </ResponseField>
    <ResponseField name="bytes" type="integer">
      The size of the object in bytes.
    </ResponseField>
    <ResponseField name="versionId" type="string">
      The version ID of the object being uploaded.
    </ResponseField>
    <ResponseField name="objectCategory" type="string">
      The category of the object.
    </ResponseField>
    <ResponseField name="fileType" type="string">
      The file type of the object.
    </ResponseField>
    <ResponseField name="isSynthetic" type="boolean">
      Indicates whether the object is synthetic.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object being uploaded.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket into which the object is being uploaded.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="PostObject" type="28">
  <Expandable title="Properties">
    <ResponseField name="action" type="'PostObject'">
      The type of action, which is 'PostObject'.
    </ResponseField>
    <ResponseField name="bytes" type="integer">
      The size of the object in bytes.
    </ResponseField>
    <ResponseField name="versionId" type="string">
      The version ID of the object being posted.
    </ResponseField>
    <ResponseField name="objectCategory" type="string">
      The category of the object.
    </ResponseField>
    <ResponseField name="fileType" type="string">
      The file type of the object.
    </ResponseField>
    <ResponseField name="isSynthetic" type="boolean">
      Indicates whether the object is synthetic.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object being posted.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket into which the object is being posted.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="CopyObject" type="29">
  <Expandable title="Properties">
    <ResponseField name="action" type="'CopyObject'">
      The type of action, which is 'CopyObject'.
    </ResponseField>
    <ResponseField name="versionId" type="string">
      The version ID of the object being copied.
    </ResponseField>
    <Expandable title="copySource">
      <ResponseField name="bucket" type="string">
        The name of the source bucket of the object being copied.
      </ResponseField>
      <ResponseField name="objectKey" type="string">
        The key of the source object being copied.
      </ResponseField>
      <ResponseField name="versionId" type="string">
        The version ID of the source object being copied.
      </ResponseField>
    </Expandable>
    <ResponseField name="objectKey" type="string">
      The key of the destination object.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the destination bucket.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="DeleteObject" type="30">
  <Expandable title="Properties">
    <ResponseField name="action" type="'DeleteObject'">
      The type of action, which is 'DeleteObject'.
    </ResponseField>
    <ResponseField name="versionId" type="string">
      The version ID of the object being deleted, if applicable.
    </ResponseField>
    <ResponseField name="isDeleteMarker" type="boolean">
      Indicates whether the deletion is a delete marker.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object being deleted.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket containing the object being deleted.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="DeleteObjects" type="31">
  <Expandable title="Properties">
    <ResponseField name="action" type="'DeleteObjects'">
      The type of action, which is 'DeleteObjects'.
    </ResponseField>
    <Expandable title="deletedObjects">
      <ResponseField name="objectKey" type="string">
        The key of the object being deleted.
      </ResponseField>
      <ResponseField name="versionId" type="string">
        The version ID of the object being deleted, if applicable.
      </ResponseField>
      <ResponseField name="isDeleteMarker" type="boolean">
        Indicates whether the deletion is a delete marker.
      </ResponseField>
    </Expandable>
    <ResponseField name="bucket" type="string">
      The name of the bucket from which objects are being deleted.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the event.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the event.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="CreateMultipartUpload" type="32">
  <Expandable title="Properties">
    <ResponseField name="action" type="'CreateMultipartUpload'">
      The type of action, which is 'CreateMultipartUpload'.
    </ResponseField>
    <ResponseField name="uploadId" type="string">
      The unique identifier for the multipart upload.
    </ResponseField>
    <ResponseField name="objectKey" type="string">
      The key of the object being uploaded in parts.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket to which the object is being uploaded.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the multipart upload was initiated, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the multipart upload.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the multipart upload.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="CompleteMultipartUpload" type="33">
  <Expandable title="Properties">
    <ResponseField name="action" type="'CompleteMultipartUpload'">
      The type of action, which is 'CompleteMultipartUpload'.
    </ResponseField>
    <ResponseField name="uploadId" type="string">
      The unique identifier for the multipart upload being completed.
    </ResponseField>
    <ResponseField name="versionId" type="string">
      The version ID of the object being uploaded, if versioning is enabled.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket to which the multipart upload was made.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the multipart upload was completed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature involved in the event.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that completed the multipart upload.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that completed the multipart upload.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="AbortMultipartUpload" type="34">
  <Expandable title="Properties">
    <ResponseField name="action" type="'AbortMultipartUpload'">
      The type of action, which is 'AbortMultipartUpload'.
    </ResponseField>
    <ResponseField name="uploadId" type="string">
      The unique identifier for the multipart upload being aborted.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket where the multipart upload was taking place.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the multipart upload was aborted, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer involved in the multipart upload.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature related to the multipart upload.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the abort action.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the abort action.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="UploadPart" type="35">
  <Expandable title="Properties">
    <ResponseField name="action" type="'UploadPart'">
      The type of action, which is 'UploadPart'.
    </ResponseField>
    <ResponseField name="uploadId" type="string">
      The unique identifier for the multipart upload session.
    </ResponseField>
    <ResponseField name="bytes" type="integer">
      The size of the uploaded part in bytes.
    </ResponseField>
    <ResponseField name="partNumber" type="integer">
      The part number of the uploaded part.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket where the part is being uploaded.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the part upload was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer involved in the upload.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature related to the upload.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the upload part.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the upload part.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="UploadPartCopy" type="36">
  <Expandable title="Properties">
    <ResponseField name="action" type="'UploadPartCopy'">
      The type of action, which is 'UploadPartCopy'.
    </ResponseField>
    <ResponseField name="uploadId" type="string">
      The unique identifier for the multipart upload session where the part is being copied.
    </ResponseField>
    <ResponseField name="bytes" type="integer">
      The size of the part being copied in bytes.
    </ResponseField>
    <ResponseField name="partNumber" type="integer">
      The part number of the uploaded copy.
    </ResponseField>
    <Expandable title="copySource">
      <ResponseField name="bucket" type="string">
        The name of the source bucket of the object being copied.
      </ResponseField>
      <ResponseField name="objectKey" type="string">
        The key of the source object being copied.
      </ResponseField>
      <ResponseField name="versionId" type="string">
        The version ID of the source object, if applicable.
      </ResponseField>
    </Expandable>
    <ResponseField name="bucket" type="string">
      The name of the destination bucket where the copied part is being uploaded.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the copy part action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer involved in the upload.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature related to the upload.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the upload part copy.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the upload part copy.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ListParts" type="37">
  <Expandable title="Properties">
    <ResponseField name="action" type="'ListParts'">
      The type of action, which is 'ListParts'.
    </ResponseField>
    <ResponseField name="uploadId" type="string">
      The unique identifier for the multipart upload session whose parts are being listed.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket where the multipart upload is taking place.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the list parts action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer involved in the multipart upload.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature related to the multipart upload.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the list parts action.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the list parts action.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ListMultipartUploads" type="38">
  <Expandable title="Properties">
    <ResponseField name="action" type="'ListMultipartUploads'">
      The type of action, which is 'ListMultipartUploads'.
    </ResponseField>
    <ResponseField name="bucket" type="string">
      The name of the bucket where multipart uploads are being listed.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      The date and time when the list multipart uploads action was performed, in date-time format.
    </ResponseField>
    <ResponseField name="provider" type="['aws', 'backblaze', 'wasabi']">
      The storage service provider, options are ['aws', 'backblaze', 'wasabi'].
    </ResponseField>
    <ResponseField name="customerId" type="string">
      The unique identifier of the customer involved in the multipart uploads.
    </ResponseField>
    <ResponseField name="featureId" type="string">
      Identifier for the specific feature related to the multipart uploads.
    </ResponseField>
    <ResponseField name="clientType" type="string">
      The type of client that initiated the list multipart uploads action.
    </ResponseField>
    <ResponseField name="clientId" type="string">
      The unique identifier of the client that initiated the list multipart uploads action.
    </ResponseField>
  </Expandable>
</ResponseField>


{/* 
## Request Body

| Headers      |                  |
| ------------ | ---------------- |
| content-type | application/json |

```json title="Example request"
{
  "action": "ListBuckets",
  "timestamp": "2019-08-24T14:15:22Z",
  "provider": "aws",
  "customerId": "string",
  "featureId": "string",
  "clientType": "string",
  "clientId": "string"
}
```

## Code Snippets

```shell title="Shell + Curl"
curl --request POST \
  --url http://ingest.dashdive.com/s3 \
  --header 'content-type: application/json' \
  --data '{"action":"ListBuckets","timestamp":"2019-08-24T14:15:22Z","provider":"aws","customerId":"string","featureId":"string","clientType":"string","clientId":"string"}'
```

## Responses

### 200

Event processed successfully

| Headers      |                  |
| ------------ | ---------------- |
| content-type | application/json |

**Example `response` for `application/json`**

```json
{
  "message": "string"
}
```

### 400

Invalid event data

### 500

Server error */}
